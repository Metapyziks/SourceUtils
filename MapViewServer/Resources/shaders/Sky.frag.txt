precision mediump float;

varying vec3 vWorldPos;

uniform vec3 uCameraPos;
uniform samplerCube uSkyCube;

void main()
{
    vec3 dir = normalize(vWorldPos - uCameraPos);

    gl_FragColor = vec4(textureCube(uSkyCube, dir.xzy * vec3(-1, 1, 1)).rgb, 1);
}
