precision mediump float;

varying vec2 vTextureCoord;
varying vec2 vLightmapCoord;

uniform sampler2D uLightmap;
uniform vec3 uColor;

void main()
{
    vec3 color = uColor;
    vec3 lightmap = texture2D(uLightmap, vLightmapCoord).rgb;
    gl_FragColor = vec4(color * lightmap, 1);
}
